# Mimmis.no

- URL: mimmis.no
- Category: Baby, kids & toys / groceries & pet supplies
- Tier: phase1-scrape
- Chosen method: scrape — real check confirms it's viable, though not via scrapeSource() as it stands today. robots.txt (checked live) only disallows standard cart/checkout/account/forum paths, no scraper/bot block — Silent, matches SHOP-CANDIDATES.md. Fetched a real product page (besafe-izi-turn-roterbar-bilstol-fresh-black-cab) and its ToS page (`/terms`, grepped for scrap/crawl/robot/automat — no hits, no automation ban). No affiliate-network signal found, so scrape is the only confirmed path.
- Alternatives: none.
- Status: not viable 2026-07-25 — sitemap reachable, but a sampled discovery crawl through `discoverSource()` produced no priced JSON-LD offer on any page tried (several sub-sitemap/UA/path-filter combinations). Nothing to ingest until the shop's markup changes.
- Notes: The product page carries full schema.org **microdata** (`itemprop="price"` content="6995", plus `itemprop="offers"/"priceCurrency"/"availability"/"sku"/"brand"/"shippingDetails"` etc. — a complete Offer, just not in a `<script type="application/ld+json">` block). worker/sources.js's `productOffer()` only regex-scans `application/ld+json` script tags, so it would find nothing here as-is — **needs a microdata (`itemprop`) fallback parser** before this shop can actually be wired, same gap flagged for a few pet-supply shops in the parallel research pass. Baby gear (strollers, car seats, baby room furniture) — no existing pricy.no category fits; would need a new one. Candidate product URLs: mimmis.no/sagababy-kjokkenhjelper-limited, /besafe-izi-turn-roterbar-bilstol-fresh-black-cab, /hamax-outback-sykkelvogn-gronnsort, /neonate-babycall-n65, /axkid-minikid-4-max-bakovervendt-bilstol-arctic-mist-grey.
